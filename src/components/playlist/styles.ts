import styled from 'styled-components';

export const Container = styled.div`
    cursor: pointer;
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
`;

export const Image = styled.img`
    border-radius: 10px;
    margin: 15px;
    max-width: 150px;
    max-height: 150px;
`;
